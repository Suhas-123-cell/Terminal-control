# Port the WebSocket+PTY server listens on
PORT=3000

# Shared secret the mobile app must pass as ?token=... to connect.
# Generate one with: openssl rand -hex 24
TERM_TOKEN=change-me-to-a-long-random-string
